import { PlaylistDTO } from '@interfaces/Playlists';
import { TracksDTO } from '@interfaces/Tracks';

export interface RecentSearchesDTO {
    playlist: PlaylistDTO;
    tracks: TracksDTO;
}















